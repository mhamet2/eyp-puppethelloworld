class <%= metadata.name %>::install inherits <%= metadata.name %> {

  if($<%= metadata.name %>::manage_package)
  {
    package { $<%= metadata.name %>::params::package_name:
      ensure => $<%= metadata.name %>::package_ensure,
    }
  }

}
